environments {
  default {
     browserName = "gc"
     runType = "local"
     remoteUrl = "http://localhost:5656/wd/hub"
     url = "forgot password page"
  }
}

serenity {
    project.name = "Test Assignment"
    requirement.types= "epic,story,feature"
    console.colors = true
    compress.filenames = false
    take.screenshot.for.interactions = FOR_FAILURES
    take.screenshot.for.tasks = FOR_FAILURES
}
